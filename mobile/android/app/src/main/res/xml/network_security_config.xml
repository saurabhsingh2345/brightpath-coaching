<?xml version="1.0" encoding="utf-8"?>
<!--
  Plain HTTP is allowed only for local development hosts:
    10.0.2.2  Android emulator -> host machine
    localhost / 127.0.0.1  on-device tunnels (adb reverse)
    192.168.* / 10.* typical LAN addresses when testing on a real device
  Everything else must use HTTPS. Point PUBLIC_BASE_URL/API_BASE_URL at an
  https:// host in production and this file needs no changes.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="true">192.168.0.0</domain>
    </domain-config>
</network-security-config>
